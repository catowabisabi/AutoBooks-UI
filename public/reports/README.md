# Reports Folder

This folder contains auto-generated AI analysis reports.

Reports are saved in JSON format with the naming convention:
`report-{company-name}-{timestamp}.json`

These reports can be accessed via the `/reports/` public URL path.
